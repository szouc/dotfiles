
#include <stdio.h>

int main(void)
{
    // int a[][2] = {1, 2, 3, 4, 5, 6, 7, 8};
    // int *p = &a;
    // int *p2 = &a[0];
    // int *p3 = &a[0][0];
    // printf("%p", p);
    
    // b and c
    int b, c;
    b = 4;
    c = 5;
    printf("%d\n", (double)(b < c));

    return 0;
}