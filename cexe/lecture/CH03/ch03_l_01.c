#include <stdio.h>

int main(void)
{
    int i;
    int sum;
    i = 100;  // init 
    sum = 0;
    while(i >= 1) // cmp
    {

















        sum = sum + i;
        i--;  // increment
    }
    printf("sum2 = %d\n", sum);

    return 0;
}